__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# Secrets & local state — never commit
.env
config.toml
secrets/
profiles/
state.db
*.db
*.log

# Proxy list — holds credentials
proxies.txt

# Singleton lock beside state.db
state.db.lock

# Distribution list — external addresses
recipients.txt

# Exported party ids — maps wallet names to on-chain identities
wallet_addresses.txt
